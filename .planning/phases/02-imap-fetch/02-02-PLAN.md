---
phase: 02-imap-fetch
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - tests/test_fetch_integration.py
autonomous: false
requirements: [FETCH-01, FETCH-02, FETCH-04, FETCH-05]

must_haves:
  truths:
    - "Fetched real messages from Proton Mail Bridge pass through the sanitizer and produce CleanMessage objects"
    - "No PII or tracking artifacts exist in sanitizer output from real email data"
    - "The fetch-to-sanitize pipeline handles real-world MIME structures without crashing"
  artifacts:
    - path: "tests/test_fetch_integration.py"
      provides: "Integration test for live Bridge fetch + sanitize pipeline"
      min_lines: 30
  key_links:
    - from: "tests/test_fetch_integration.py"
      to: "src/fetch.py"
      via: "from src.fetch import fetch_messages"
      pattern: "from src\\.fetch import fetch_messages"
    - from: "tests/test_fetch_integration.py"
      to: "src/sanitizer.py"
      via: "from src.sanitizer import sanitize"
      pattern: "from src\\.sanitizer import sanitize"

user_setup:
  - service: proton-mail-bridge
    why: "IMAP fetch requires a running, authenticated Proton Mail Bridge instance"
    env_vars:
      - name: IMAP_HOST
        source: "Should be 127.0.0.1 (localhost)"
      - name: IMAP_PORT
        source: "Should be 1143 (Bridge default IMAP port)"
      - name: IMAP_USERNAME
        source: "Your Proton Mail email address"
      - name: IMAP_PASSWORD
        source: "Proton Mail Bridge generated password (not your Proton account password) — found in Bridge app settings"
      - name: NEWSLETTER_FOLDER
        source: "The IMAP folder name where newsletters are stored (e.g. 'Newsletters'). Check Bridge folder list."
    dashboard_config:
      - task: "Ensure Proton Mail Bridge is installed, running, and signed in"
        location: "Proton Mail Bridge desktop app"
      - task: "Ensure at least one newsletter email exists in the configured folder from the last 24 hours"
        location: "Proton Mail web app or desktop client"
---

<objective>
Create a live integration test that connects to a real Proton Mail Bridge, fetches messages, and passes them through the sanitizer. Then verify the full pipeline with user confirmation.

Purpose: Unit tests with mocks prove the code logic; this plan proves it works against the real Bridge with real email data. This is the phase gate — Phase 2 success criterion #4 requires real messages to produce clean CleanMessage objects.
Output: Integration test file + human verification that real newsletters flow through the pipeline cleanly.
</objective>

<execution_context>
@/Users/darron/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-imap-fetch/02-01-SUMMARY.md

<interfaces>
From src/fetch.py (created in Plan 01):
```python
def fetch_messages(config: dict) -> list[RawMessage]:
    """Fetch newsletter messages from Proton Mail Bridge."""
```

From src/sanitizer.py:
```python
def sanitize(raw: RawMessage, config: SanitizerConfig) -> CleanMessage:
    """Convert RawMessage to CleanMessage with all privacy protections applied."""
```

From src/config.py:
```python
def load_config() -> dict:
    """Load config from .env. Requires IMAP_HOST, IMAP_PORT, IMAP_USERNAME, IMAP_PASSWORD."""

def load_sanitizer_config() -> SanitizerConfig:
    """Load SanitizerConfig from .env."""
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create integration test for fetch-to-sanitize pipeline</name>
  <files>tests/test_fetch_integration.py</files>
  <action>
    Create `tests/test_fetch_integration.py` with a pytest marker for integration tests that require a live Bridge.

    The file should:
    1. Add a `@pytest.mark.skipif` decorator that skips when env var `SIGNALS_INTEGRATION=1` is NOT set (so normal `pytest` runs skip this file)
    2. Import `load_config`, `load_sanitizer_config` from `src.config`
    3. Import `fetch_messages` from `src.fetch`
    4. Import `sanitize` from `src.sanitizer`
    5. Import `RawMessage`, `CleanMessage` from `src.models`

    Tests to write:

    **test_fetch_returns_raw_messages:**
    - Call `load_config()` then `fetch_messages(config)`
    - Assert result is a list
    - Assert each item is a RawMessage instance
    - Assert each RawMessage has a non-empty subject and sender
    - Assert at least one message has body_html (newsletters are almost always HTML)

    **test_fetched_messages_sanitize_cleanly:**
    - Fetch messages, then for each RawMessage call `sanitize(raw, sanitizer_config)`
    - Assert each result is a CleanMessage instance
    - Assert each CleanMessage has non-empty clean_text
    - Assert each CleanMessage.sender_domain contains a dot (is a domain, not a full address)
    - Assert the user's email address (from config) does NOT appear in any CleanMessage field

    **test_fetch_respects_time_window:**
    - Fetch with default config (24h window)
    - If any messages returned, parse each Date header and assert all are within ~25h of now (allowing some slack for SINCE date-only precision)

    Use `SIGNALS_INTEGRATION=1` env var as the gate. Document in a module docstring how to run:
    ```
    SIGNALS_INTEGRATION=1 pytest tests/test_fetch_integration.py -x -v
    ```
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('tests/test_fetch_integration.py').read()); print('syntax OK')"</automated>
  </verify>
  <done>Integration test file exists with 3 tests, skipped by default, runnable with SIGNALS_INTEGRATION=1</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify live Bridge integration</name>
  <files>tests/test_fetch_integration.py</files>
  <action>
    Human verifies the fetch-to-sanitize pipeline works against a live Proton Mail Bridge.

    What was built: Complete IMAP fetch module (Plan 01) and integration test (Task 1 above). The fetch module connects to Proton Mail Bridge, fetches newsletters by UID, parses MIME bodies, and produces RawMessage objects. The sanitizer (Phase 1) converts them to CleanMessage objects with all PII and tracking stripped.

    Steps to verify:
    1. Ensure Proton Mail Bridge is running and authenticated
    2. Ensure `.env` file has correct IMAP_HOST, IMAP_PORT, IMAP_USERNAME, IMAP_PASSWORD, and NEWSLETTER_FOLDER
    3. Ensure at least one newsletter exists in the configured folder from the last 24 hours
    4. Activate the virtual environment: `source .venv/bin/activate`
    5. Run the integration tests:
       ```
       SIGNALS_INTEGRATION=1 pytest tests/test_fetch_integration.py -x -v
       ```
    6. Expected: All 3 tests pass — messages fetched, sanitized cleanly, time window respected
    7. If tests fail with ConnectionRefusedError: Bridge is not running or wrong port
    8. If tests fail with IMAP4.error: wrong credentials (use Bridge-generated password, not Proton account password)
    9. If tests return 0 messages: check NEWSLETTER_FOLDER name matches Bridge folder exactly, or ensure a newsletter arrived in the last 24 hours
  </action>
  <verify>
    <automated>python -c "import tests.test_fetch_integration; print('module importable')"</automated>
  </verify>
  <done>User confirms integration tests pass against live Proton Mail Bridge with real newsletter data</done>
</task>

</tasks>

<verification>
```bash
# Unit tests still pass (Plan 01)
pytest tests/test_fetch.py -x -q

# Full suite (skips integration by default)
pytest tests/ -q

# Integration test (requires live Bridge + SIGNALS_INTEGRATION=1)
# SIGNALS_INTEGRATION=1 pytest tests/test_fetch_integration.py -x -v
```
</verification>

<success_criteria>
- Integration test file exists and is skipped by default in normal pytest runs
- With SIGNALS_INTEGRATION=1 and a running Bridge, all 3 integration tests pass
- Real newsletters produce CleanMessage objects with no PII or tracking artifacts
- Full unit test suite (23 tests) remains green
</success_criteria>

<output>
After completion, create `.planning/phases/02-imap-fetch/02-02-SUMMARY.md`
</output>
